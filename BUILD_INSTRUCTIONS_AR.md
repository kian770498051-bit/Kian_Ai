# تعليمات سريعة

افتح مجلد KianAI في Android Studio، ثم ثبّت SDK 35 إن لزم، وبعدها عدّل `BACKEND_URL` في `app/build.gradle` إلى خادمك. استخدم Build > Build APK(s).

مفتاح OpenAI لا يوضع في التطبيق. ضعه في متغير البيئة `OPENAI_API_KEY` على الخادم.
